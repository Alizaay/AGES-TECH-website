/**
 * Future-ready data-fetching provider placeholder.
 * Swap for React Query / SWR when backend APIs are live.
 */
export const QueryProvider = ({ children }) => children

export default QueryProvider